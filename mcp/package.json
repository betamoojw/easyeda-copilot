{
  "name": "easyeda-copilot-mcp",
  "version": "1.1.7",
  "mcpName": "io.github.biosshot/easyeda-copilot",
  "description": "MCP server for EasyEDA Copilot",
  "type": "module",
  "license": "MIT",
  "repository": {
    "url": "https://github.com/biosshot/easyeda-copilot",
    "type": "git"
  },
  "bin": {
    "easyeda-copilot-mcp": "./dist/index.js"
  },
  "files": [
    "dist",
    "docs",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsup",
    "test:routing-adapter": "npm run build && node scripts/check-routing-adapter.mjs",
    "test:operations": "npm run build && node scripts/check-operation-manager.mjs",
    "clean": "rimraf dist",
    "typecheck": "tsc --noEmit",
    "inspect": "npm run build && npx @modelcontextprotocol/inspector node dist/index.js",
    "check": "npm run typecheck && npm run build && node scripts/check-routing-adapter.mjs && node scripts/check-operation-manager.mjs",
    "prepack": "npm run build",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "eda-copilot-router": "0.2.1",
    "find-up": "^5.0.0",
    "sharp": "^0.35.0",
    "ws": "^8.20.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@copilot/shared": "file:../packages/shared",
    "@types/node": "^25.9.1",
    "@types/sharp": "^0.31.1",
    "@types/ws": "^8.18.1",
    "rimraf": "^6.1.2",
    "tsup": "^8.5.1",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=20"
  }
}
